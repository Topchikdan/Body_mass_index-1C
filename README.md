# Конфигурация «ИМТ» 1С (BMI-1C)

Заготовка конфигурации 1С с ресурсом для будущего расчёта индекса массы тела (ИМТ).

## О проекте

На данный момент конфигурация находится на начальном этапе разработки и содержит единственный объект — общую картинку **«ИМТ»**, предназначенную для использования в качестве иконки/изображения будущей обработки или отчёта по расчёту индекса массы тела.

Бизнес-логика (расчёт ИМТ, ввод роста/веса пользователя, интерпретация результата) пока не реализована.

Исходники выгружены штатным механизмом платформы (**Конфигурация → Выгрузить конфигурацию в файлы...**) в текстовом/XML-виде, что позволяет вести историю изменений в git без хранилища конфигурации (актуально для базовых версий 1С, где хранилище недоступно).

## Состав конфигурации

### Общие картинки

| Объект | Назначение |
|---|---|
| **ИМТ** | Изображение для будущей обработки/отчёта расчёта индекса массы тела |

## Требования

- Платформа 1С:Предприятие 8.3 (для открытия и редактирования конфигурации в Конфигураторе)

## Как обновить репозиторий после изменений в конфигурации

1. Откройте базу в режиме **Конфигуратор**.
2. Выполните **Конфигурация → Выгрузить конфигурацию в файлы...**, указав эту же папку.
3. Зафиксируйте изменения:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## Как развернуть конфигурацию из репозитория

1. Создайте новую информационную базу в 1С (или используйте существующую).
2. В Конфигураторе выполните **Конфигурация → Загрузить конфигурацию из файлов...**, указав путь к этой папке.
3. Обновите конфигурацию базы данных (**F7**).

## Планы по развитию

- [ ] Добавить обработку для ввода роста и веса
- [ ] Реализовать расчёт индекса массы тела
- [ ] Добавить интерпретацию результата (норма/недостаток/избыток веса)

## Лицензия

Не указана.
